% Note: We do not specify the package options at all in this file.
% They are moved to common-patches.sty because conference style files may load them first without options,
% causing option clashes.
\input{styles/common-patches.sty}

% \input{styles/grammarly.sty} % grammarly style removes the page number and hyphenations, easy to paste the result to grammarly
\input{styles/aaai.sty}
% \input{styles/ijcai.sty}
% \input{styles/ecai.sty}
% \input{styles/jair.sty}
% \input{styles/neurips.sty}
% \input{styles/icml.sty}
% \input{styles/iclr.sty}
% \input{styles/naacl.sty}
% \input{styles/cvpr.sty}
% \input{styles/jsai.sty}

\makeatletter
\@ifpackageloaded{theapa}{%
 % jair; do not load
}{
 \usepackage{natbib}
}
\makeatother

\usepackage{xparse}

% AMS packages
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{amsfonts}

% Tables, Figures
\usepackage{tabularx}
\usepackage{booktabs}   % Recommended in nips/icml, ``for professional tables'', enables toprule/midrule/bottomrule.
\usepackage{multirow}
\usepackage{subfigure}

% for \dbend
\usepackage{manfnt}

% spacing, compression
\usepackage{xspace}
\usepackage{relsize}
\usepackage{adjustbox}
\usepackage{diagbox}

% Recommended in nips/icml.
% ``Subliminal refinements towards typographical perfection''.
% See http://www.khirevich.com/latex/microtype/ for more detailed explanations.
% It may also help compressing the paper.
\usepackage{microtype}

\usepackage{graphicx}
\usepackage{url}
\usepackage{comment}
\usepackage{xcolor}
\usepackage{colortbl}

%% Algorithms. These options may cause conflicts, enable them by your own needs
% \usepackage{algorithm}
% \usepackage{algorithmic} % uses \STATE, \COMMENT
% \usepackage{algpseudocode} % uses \State, \Comment

% \usepackage{hyperref} % not allowed in aaai, but allowed in icml, nips

% for conference submission with supplements. AAAI camera-ready does not allow them
\usepackage{xr}

% custom commands. The use of \input is intentional ---
% they will be inlined by the camera-ready script.

\input{styles/common-math.sty}
\input{styles/common-general.sty}
\input{styles/common-abbrev.sty}
\input{parts/strips.sty}

% Note: Minted package works by calling an external program called pygment,
% which requires TeX to be invoked with --shell-escape flag.
% Naturally, many services including Arxiv do not set the flag.
% To make it work on Arxiv, usually you must manually follow:
% 1. Typeset the document with finalizecache option, which saves the cached pygment output, then
% \usepackage[finalizecache,cachedir=.]{minted}
% 2. Re-typeset the document with frozencache option to read from the cache, which does not require --shell-escape.
% \usepackage[frozencache,cachedir=.]{minted}
%
% Our camera-ready script automates this process.

\iftoggle{dev}{%
  \IfFileExists{\jobname.needpyg}{% how else to pass information to latex??
    % finalizing build
    \usepackage[finalizecache,cachedir=.]{minted}%
  }{%
    % standard build
    \usepackage{minted}%
  }%
}{%
  % submission build
  \usepackage[frozencache,cachedir=.]{minted}%
}



\hyphenation{bar-man air-port cyber-sec driver-log floor-tile free-cell
no-mystery open-stacks parc-printer path-ways peg-sol pipes-world
no-tankage scan-alyzer soko-ban tidy-bot visit-all zeno-travel tie-break-ing}

\frenchspacing
\setlength{\pdfpagewidth}{8.5in}
\setlength{\pdfpageheight}{11in}
\setcounter{secnumdepth}{2}

\allowdisplaybreaks

